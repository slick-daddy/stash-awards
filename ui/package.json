{
  "name": "stash-awards-ui",
  "version": "0.1.0",
  "private": true,
  "description": "UI for the Stash Awards plugin. Builds plugin/awards.js and plugin/awards.css.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/react": "^17.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.5.0"
  }
}
